import React from 'react';

import { Wrapper, Container, Rectangle } from './styles';

const ProductListPlaceholder = () => {
  return (
    <Wrapper>
      <Container>
        <Rectangle />
      </Container>
    </Wrapper>
  );
};

export default ProductListPlaceholder;
