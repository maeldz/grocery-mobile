import React from 'react';

import { Container, Rectangle } from './styles';

const ImageSlidePlaceholder = () => {
  return (
    <Container>
      <Rectangle />
    </Container>
  );
};

export default ImageSlidePlaceholder;
